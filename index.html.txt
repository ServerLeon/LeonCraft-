<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LeonCraft | سرور ماینکرفت</title>
    <style>
        @font-face {
            font-family: 'Koolak';
            src: url('Koolak.woff2') format('woff2');
            font-weight: normal;
            font-style: normal;
        }

         {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Koolak', Tahoma, sans-serif;
            background: linear-gradient(135deg, #1a0a00 0%, #2d1c15 50%, #1a0a00 100%);
            color: #fff;
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
        }

        / ذرات شناور در پس‌زمینه /
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .particle {
            position: absolute;
            width: 4px;
            height: 4px;
            background: #ffd54f;
            border-radius: 50%;
            animation: float 15s infinite;
            opacity: 0.3;
        }

        @keyframes float {
            0%, 100% { transform: translateY(100vh) scale(0); opacity: 0; }
            10% { opacity: 0.3; }
            90% { opacity: 0.3; }
            100% { transform: translateY(-100vh) scale(1); opacity: 0; }
        }

        / ناوبری /
        nav {
            background: rgba(30, 15, 5, 0.95);
            backdrop-filter: blur(20px);
            padding: 15px 30px;
            position: sticky;
            top: 0;
            z-index: 1000;
            border-bottom: 2px solid #8d6e63;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        .logo {
            font-size: 28px;
            font-weight: bold;
            background: linear-gradient(45deg, #ffd54f, #ff8f00);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: none;
            letter-spacing: 2px;
        }

        .nav-links {
            display: flex;
            gap: 25px;
            list-style: none;
        }

        .nav-links a {
            color: #fff;
            text-decoration: none;
            font-size: 14px;
            transition: all 0.3s;
            padding: 8px 15px;
            border-radius: 5px;
            border: 1px solid transparent;
        }

        .nav-links a:hover {
            background: rgba(255, 179, 0, 0.1);
            border-color: #ffb300;
            color: #ffb300;
        }

        .server-status {
            background: rgba(76, 175, 80, 0.2);
            border: 1px solid #4caf50;
            padding: 8px 20px;
            border-radius: 20px;
            font-size: 12px;
            color: #4caf50;
        }

        / هدر اصلی /
        .hero {
            text-align: center;
            padding: 80px 20px;
            position: relative;
            z-index: 2;
            background: radial-gradient(circle, rgba(255,179,0,0.1) 0%, transparent 70%);
        }

        .hero h1 {
            font-size: 48px;
            margin-bottom: 20px;
            animation: glitch 3s infinite;
        }

        @keyframes glitch {
            0%, 90%, 100% { transform: translate(0); }
            92% { transform: translate(-2px, 2px); }
            94% { transform: translate(2px, -2px); }
            96% { transform: translate(-2px, -2px); }
            98% { transform: translate(2px, 2px); }
        }

        .hero p {
            font-size: 16px;
            color: #a5d6a7;
            margin-bottom: 30px;
        }

        / دکمه‌ها */
        .btn {
            display: inline-block;
            padding: 15px 40px;
            border-radius: 30px;
            text-decoration: none;
            font-size: 14px;
            font-weight: bold;

transition: all 0.3s;
            margin: 10px;
            cursor: pointer;
            border: none;
        }

        .btn-primary {
            background: linear-gradient(135deg, #ff8f00, #ff6f00);
            color: #fff;
            box-shadow: 0 5px 20px rgba(255, 111, 0, 0.4);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(255, 111, 0, 0.6);
        }

        .btn-secondary {
            background: transparent;
            color: #ffb300;
            border: 2px solid #ffb300;
        }

        .btn-secondary:hover {
            background: rgba(255, 179, 0, 0.1);
        }

        /* محتوای اصلی /
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            position: relative;
            z-index: 2;
        }

        / کارت‌های رنک /
        .section-title {
            text-align: center;
            font-size: 32px;
            margin: 50px 0 30px;
            color: #ffb74d;
            position: relative;
        }

        .section-title::after {
            content: "";
            display: block;
            width: 100px;
            height: 3px;
            background: linear-gradient(90deg, transparent, #ffb300, transparent);
            margin: 15px auto;
        }

        .ranks-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
            margin: 40px 0;
        }

        .rank-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 30px 20px;
            text-align: center;
            transition: all 0.4s;
            position: relative;
            overflow: hidden;
        }

        .rank-card:hover {
            transform: translateY(-10px);
            border-color: #ffb300;
            box-shadow: 0 20px 40px rgba(255, 179, 0, 0.2);
        }

        .rank-icon {
            font-size: 40px;
            margin-bottom: 15px;
            display: block;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

        .rank-card h3 {
            font-size: 18px;
            margin: 10px 0;
            color: #ffb300;
        }

        .rank-card .price {
            font-size: 16px;
            color: #81c784;
            background: rgba(0, 0, 0, 0.5);
            padding: 8px 15px;
            border-radius: 20px;
            border: 1px solid #4caf50;
            display: inline-block;
            margin-top: 10px;
        }

        / نوار پیشرفت رنک /
        .rank-progress {
            width: 100%;
            height: 4px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 2px;
            margin-top: 15px;
        }

        .rank-progress-bar {
            height: 100%;
            border-radius: 2px;
            transition: width 1s;
        }

        / رنگ‌های مخصوص هر رنک /
        .rank-vip { border-top: 3px solid #4caf50; }
        .rank-mvp { border-top: 3px solid #2196f3; }
        .rank-knight { border-top: 3px solid #9c27b0; }
        .rank-king { border-top: 3px solid #ff9800; }
        .rank-superme { border-top: 3px solid #f44336; }
        .rank-leon { border-top: 3px solid #00bcd4; }
        .rank-leon-star { border-top: 3px solid #ffeb3b; }

        / گیم‌مودها */
        .gamemodes-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 20px;
            margin: 40px 0;
        }

        .gamemode-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 25px;
            text-align: center;
            transition: all 0

.3s;
            position: relative;
        }

        .gamemode-card:hover {
            background: rgba(255, 255, 255, 0.1);
            transform: scale(1.05);
        }

        .gamemode-icon {
            font-size: 40px;
            display: block;
            margin-bottom: 15px;
        }

        .gamemode-card h3 {
            font-size: 16px;
            margin: 10px 0;
        }

        .status-badge {
            display: inline-block;
            padding: 5px 15px;
            border-radius: 15px;
            font-size: 11px;
            margin-top: 10px;
        }

        .status-offline {
            background: rgba(244, 67, 54, 0.2);
            border: 1px solid #f44336;
            color: #f44336;
        }

        /* فوتر /
        footer {
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(20px);
            padding: 30px;
            text-align: center;
            margin-top: 50px;
            border-top: 2px solid #8d6e63;
        }

        .social-links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 20px 0;
        }

        .social-link {
            width: 50px;
            height: 50px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            text-decoration: none;
            color: #fff;
            font-size: 20px;
            transition: all 0.3s;
        }

        .social-link:hover {
            background: #ffb300;
            transform: rotate(360deg);
        }

        / ریسپانسیو */
        @media (max-width: 768px) {
            nav {
                flex-direction: column;
                gap: 15px;
            }

            .nav-links {
                flex-wrap: wrap;
                justify-content: center;
                gap: 10px;
            }

            .hero h1 {
                font-size: 32px;
            }

            .ranks-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .gamemodes-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 480px) {
            .ranks-grid {
                grid-template-columns: 1fr;
            }

            .gamemodes-grid {
                grid-template-columns: 1fr;
            }

            .hero {
                padding: 40px 10px;
            }
        }
    </style>
</head>
<body>
    <!-- ذرات پس‌زمینه -->
    <div class="particles" id="particles"></div>

    <!-- ناوبری -->
    <nav>
        <div class="logo">🦁 LEONCRAFT</div>
        <ul class="nav-links">
            <li><a href="#">خانه</a></li>
            <li><a href="#">فروشگاه</a></li>
            <li><a href="#">انجمن</a></li>
            <li><a href="#">قوانین</a></li>
            <li><a href="#">تماس با ما</a></li>
        </ul>
        <div class="server-status">
            🟢 آنلاین | ۱۲۴ بازیکن
        </div>
    </nav>

    <!-- هدر -->
    <div class="hero">
        <h1>به سرور LeonCraft خوش آمدید</h1>
        <p>بهترین سرور ماینکرفت ایران با گیم‌مودهای متنوع</p>
        <div>
            <a href="#" class="btn btn-primary">همین حالا شروع کنید</a>
            <a href="#" class="btn btn-secondary">اطلاعات بیشتر</a>
        </div>
    </div>

    <div class="container">
        <!-- رنک‌ها -->
        <h2 class="section-title">🏅 رنک‌های ویژه</h2>
        <div class="ranks-grid">
            <div class="rank-card rank-vip">
                <span class="rank-icon">💎</span>
                <h3>VIP</h3>
                <div class="rank-progress"><div class="rank-progress-bar" style="width: 20%; background: #4caf50;"></div></div>
                <p class="price">۴۰,۰۰۰ تومان</p>
            </div>

            <div class="rank-card rank-mvp">
                <span class="rank-icon">⭐</span>
                <h3>MVP</h3>
                <div class="rank-progress"><div class="rank-progress-bar" style="width: 35%; backgroun

d: #2196f3;"></div></div>
                <p class="price">۶۵,۰۰۰ تومان</p>
            </div>

            <div class="rank-card rank-knight">
                <span class="rank-icon">⚔️</span>
                <h3>Knight</h3>
                <div class="rank-progress"><div class="rank-progress-bar" style="width: 55%; background: #9c27b0;"></div></div>
                <p class="price">۱۱۰,۰۰۰ تومان</p>
            </div>

            <div class="rank-card rank-king">
                <span class="rank-icon">👑</span>
                <h3>King</h3>
                <div class="rank-progress"><div class="rank-progress-bar" style="width: 60%; background: #ff9800;"></div></div>
                <p class="price">۱۲۰,۰۰۰ تومان</p>
            </div>

            <div class="rank-card rank-superme">
                <span class="rank-icon">🔥</span>
                <h3>Superme</h3>
                <div class="rank-progress"><div class="rank-progress-bar" style="width: 70%; background: #f44336;"></div></div>
                <p class="price">۱۴۰,۰۰۰ تومان</p>
            </div>

            <div class="rank-card rank-leon">
                <span class="rank-icon">🦁</span>
                <h3>Leon</h3>
                <div class="rank-progress"><div class="rank-progress-bar" style="width: 80%; background: #00bcd4;"></div></div>
                <p class="price">۱۵۵,۰۰۰ تومان</p>
            </div>

            <div class="rank-card rank-leon-star">
                <span class="rank-icon">🌟</span>
                <h3>Leon-Star</h3>
                <div class="rank-progress"><div class="rank-progress-bar" style="width: 100%; background: #ffeb3b;"></div></div>
                <p class="price">۱۹۹,۰۰۰ تومان</p>
            </div>
        </div>

        <!-- گیم‌مودها -->
        <h2 class="section-title">🎮 گیم‌مودها</h2>
        <div class="gamemodes-grid">
            <div class="gamemode-card">
                <span class="gamemode-icon">⛏️</span>
                <h3>SkyMine</h3>
                <span class="status-badge status-offline">آفلاین</span>
            </div>

            <div class="gamemode-card">
                <span class="gamemode-icon">🌳</span>
                <h3>Survival</h3>
                <span class="status-badge status-offline">آفلاین</span>
            </div>

            <div class="gamemode-card">
                <span class="gamemode-icon">🛏️</span>
                <h3>BedWars</h3>
                <span class="status-badge status-offline">آفلاین</span>
            </div>

            <div class="gamemode-card">
                <span class="gamemode-icon">⚔️</span>
                <h3>Practice</h3>
                <span class="status-badge status-offline">آفلاین</span>
            </div>

            <div class="gamemode-card">
                <span class="gamemode-icon">🏹</span>
                <h3>SkyWars</h3>
                <span class="status-badge status-offline">آفلاین</span>
            </div>

            <div class="gamemode-card">
                <span class="gamemode-icon">🏃‍♂️</span>
                <h3>Parkour</h3>
                <span class="status-badge status-offline">آفلاین</span>
            </div>
        </div>
    </div>

    <!-- فوتر -->
    <footer>
        <div class="social-links">
            <a href="#" class="social-link">📱</a>
            <a href="#" class="social-link">💬</a>
            <a href="#" class="social-link">📷</a>
            <a href="#" class="social-link">🎮</a>
        </div>
        <p>©️ 1405 LeonCraft - تمامی حقوق محفوظ است.</p>
        <p style="font-size: 12px; margin-top: 10px; color: #8d6e63;">IP: LeonCraft.ir</p>
    </footer>

    <!-- اسکریپت ذرات -->
    <script>
        const particlesContainer = document.getElementById('particles');
        for (let i = 0; i < 50; i++) {
            const particle = document.createElement('div');
            particle.className = 'particle';
            particle.style.left = Math.random()  100 + '%';
            particle.style.animationDelay = Math.random()  15 + 's';
            particle.style.animationDuration =

(Math.random() * 10 + 10) + 's';
            particlesContainer.appendChild(particle);
        }
    </script>
</body>
</html>